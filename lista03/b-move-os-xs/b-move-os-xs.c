#include <stdio.h>

#define TAM_ENTRADA 101

char saida[TAM_ENTRADA];

char* move_os_xs(char* entrada)
{

}

int main(void)
{
    char entrada[TAM_ENTRADA];
    
    scanf("%100s", entrada);

    printf("%s\n", move_os_xs(entrada));

    return 0;
}
